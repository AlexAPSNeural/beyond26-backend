import { getPool } from '../src/db.js';import bcrypt from 'bcryptjs';import dotenv from 'dotenv';import { v4 as uuid } from 'uuid';dotenv.config();async function run(){const pool=getPool();if(!pool){console.error('DATABASE_URL not set');process.exit(1)}const email='admin@b26.com';const hash=await bcrypt.hash('Password123!',10);const id=uuid();await pool.query('INSERT INTO users(id,email,first_name,last_name,role,password_hash) VALUES ($1,$2,$3,$4,$5,$6) ON CONFLICT (email) DO NOTHING',[id,email,'Admin','User','admin',hash]);console.log('Seeded user: admin@b26.com / Password123!');process.exit(0)}run().catch(e=>{console.error(e);process.exit(1)})